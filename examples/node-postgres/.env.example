# Example environment for the Node + Postgres collector.
# Copy to `.env` and adjust the values.

# Any Postgres-compatible connection string works
# (local Postgres, Neon, Supabase, Timescale, QuestDB, …).
DATABASE_URL=postgres://user:password@localhost:5432/unifeather

# Port the collector listens on.
PORT=3000
