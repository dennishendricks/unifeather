{
  "name": "unifeather",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Lightweight, privacy-friendly, tree-shakeable website analytics — tracker, endpoint and dashboard.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '@unifeather/core' build && bun run --filter '@unifeather/tracker' build && bun run --filter '@unifeather/server' build && bun run --filter '@unifeather/dashboard' build",
    "dev": "bun run --filter '*' dev",
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun test",
    "clean": "rm -rf packages/*/dist packages/*/*.tsbuildinfo"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
